### save the Credentials
create `~/.config/gh/hosts.yml` and add this with your credentials
```yaml
github.com:
    git_protocol: https
    users:
        <GITHUB_USER>:
            oauth_token: <TOKEN>
    user: <GITHUB_USER>
    oauth_token: <TOKEN>

```
